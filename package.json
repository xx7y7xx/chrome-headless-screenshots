{
  "name": "chrome-headless-screenshots",
  "version": "1.1.0",
  "description": "",
  "main": "snapshot.js",
  "scripts": {
    "start": "npm run snapshot",
    "clean": "rimraf output/ && mkdirp output/",
    "snapshot": "npm run clean && node snapshot.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "chrome-remote-interface": "^0.23.0",
    "delay": "^2.0.0",
    "jimp": "^0.2.28",
    "minimist": "^1.2.0",
    "mz": "^2.6.0"
  },
  "devDependencies": {
    "mkdirp": "^0.5.1",
    "rimraf": "^2.6.1"
  }
}
